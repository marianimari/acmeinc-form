import React from "react";

const TitleProduct = (props) => {
  return (
    <div className="font-bold text-dark-100 leading-4 text-sm mt-2">
      {props.title}
    </div>
  );
};

export default TitleProduct;
